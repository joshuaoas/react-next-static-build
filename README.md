# react-next-static-build
This repo uses Next.js and builds a static site out of markdown and Frontmatter, using Netlify CMS to update.


## To run
```
nvm use 15.3.0
yarn
yarn run dev
yarn run build // to build
```
